# Developer Tools

Developer-only scripts and utilities belong here, for example flashing helpers,
BOM checks, calibration-data generation, measurement capture, or CI support.

Do not move product functionality into `tools/`. A tool should automate an
engineering workflow rather than become a hidden runtime dependency.
