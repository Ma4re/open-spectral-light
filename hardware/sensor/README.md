# Sensor Hardware

Responsibility: optional physical measurement that justifies a dedicated module,
such as spectral/color sensing, high-bandwidth flicker sensing, or reference
temperature measurement.

A base lamp must operate safely and usefully without this module. Sensor selection
is driven by the metrics it can honestly support after calibration, not by the
number of channels printed on a datasheet.
