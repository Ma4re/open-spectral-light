# System and HIL Tests

This directory is reserved for verification that spans firmware modules or
requires real/simulated physical systems: characterization, HIL fixtures,
thermal/flicker experiments, and acceptance scenarios.

Portable controller unit/component tests remain with the controller firmware.
Do not add HIL infrastructure until a real hardware revision exists.
